{
  "name": "bimbles-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "cd frontend && bun run build && cp -r dist ../backend/dist",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "hono": "^4.12.31",
    "groq-sdk": "^0.8.0",
    "ioredis": "^5.3.2",
    "limiter": "^3.0.0",
    "prom-client": "^14.2.0"
  },
  "devDependencies": {
    "bun-types": "^1.2.0",
    "typescript": "^5.6.0"
  }
}
